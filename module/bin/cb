#!/system/bin/sh
# Thin wrapper；推荐使用外部入口 /data/adb/certbridge/cb
MODDIR=${0%/*}/..
case "$MODDIR" in
  /data/adb/certbridge)
    MODDIR=/data/adb/modules/CertBridge
    ;;
esac
exec sh "$MODDIR/bin/cert_manager.sh" "$@"
