#!/system/bin/sh
# Hide assist actions (split: probe / clear / register)
# shellcheck disable=SC1090
. "$LIBDIR/hide_probe.sh"
# shellcheck disable=SC1090
. "$LIBDIR/hide_clear.sh"
# shellcheck disable=SC1090
. "$LIBDIR/hide_register.sh"
