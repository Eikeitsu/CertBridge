# 证书桥配置
# 内置证书开关：1=启用 0=禁用
# mount_mode：compatible=完整兼容（默认，运行时 bind）| magic=轻量 Magic Mount（仅叠 addon）
# tmpfs_style：short=短路径 .fs0/.fs1（默认）| legacy=sys-ca-merge{,-hot}
schema_version=3
reqable=1
proxypin=1
mount_mode=compatible
tmpfs_style=short
